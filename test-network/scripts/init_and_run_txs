#!/bin/sh -euC

scripts_dir=$(dirname "$(readlink -f "$0")")
. "$scripts_dir/common.sh"

TEST_INPUTS_DIR=${TEST_INPUTS_DIR:-$scripts_dir/../../smart-contract/hyperledger-fabric/v2/java/transaction_data/test_inputs}

assert_cmd "$HTTPIE"
assert_cmd "$JQ"

TOKEN=$(get_token)
invoke method=init args:='[]'
invoke method=doNewOrder args:="$(cat "$TEST_INPUTS_DIR/doNewOrderInput")"
